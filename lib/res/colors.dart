import 'package:flutter/rendering.dart';

class LightAppColors {
  LightAppColors._();
  // Prevents instantiation

  // Background color
  static const background = Color(0xffffffff);

  static const primary = Color(0xffed7b2a);
  static const onPrimary = Color(0xffffffff);

  static const surfaceVariant = Color(0xFFF3F4F6);
  static const onSurfaceVariant = Color(0xFF6B7280);

  static const secondary = Color(0xfff3ece7);
  static const onSecondary = Color(0xff52392c);

  static const surface = Color(0xffffffff);
  static const onSurface = Color(0xff1b130d);

  static const primaryContainer = Color(0xffffe0c2);
  static const onPrimaryContainer = Color(0xff52392c);

  // Text colors
  static const textPrimary = Color(0xff1b130d);      // Main text color
  static const textSecondary = Color(0xFF6B7280);    // Secondary text color
  static const textTertiary = Color(0xff9a6c4c);     // Tertiary/label text color
  static const textError = Color(0xffef4444);        // Error text color
  static const textOnPrimary = Color(0xffffffff);    // Text on primary backgrounds
  static const textOnSecondary = Color(0xff52392c);  // Text on secondary backgrounds
}